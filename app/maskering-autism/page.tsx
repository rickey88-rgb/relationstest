import AutismGuide, { autismMetadata } from "../_components/AutismGuide";

export const metadata = autismMetadata("maskering-autism");

export default function Page() {
  return <AutismGuide slug="maskering-autism" />;
}
